the ArgoUML class diagram goes into 'class-diagram'
after exporting the diagram into .java files, they are put into 'java-files'
after compiling the .java files into .class files, they are put into 'compiled-files'
